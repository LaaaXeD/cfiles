#include <stdio.h>

int main() {
    // array = a data structure that can store a fixed number of values of the same type.

    double prices[] = {5.0,10.0,15.0,20.0}; // Declare and initialize an array of doubles
    
    printf("$%.2lf", prices[0]); // Print the first element of the array


    return 0;
}